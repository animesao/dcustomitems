name: Build & Release

on:
  push:
    branches: [ main ]
    tags: [ 'v*' ]

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Set up JDK 17
      uses: actions/setup-java@v4
      with:
        java-version: '17'
        distribution: 'temurin'

    - name: Get version from tag
      id: version
      run: |
        if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
          VERSION="${GITHUB_REF#refs/tags/v}"
        else
          VERSION="dev"
        fi
        echo "version=$VERSION" >> $GITHUB_OUTPUT
        echo "Version: $VERSION"

    - name: Update project version in pom.xml
      run: |
        # Only replace the first <version> tag (project version)
        sed -i "0,/<version>.*<\/version>/s//<version>${{ steps.version.outputs.version }}<\/version>/" pom.xml
        echo "Updated pom.xml:"
        head -15 pom.xml

    - name: Build
      run: mvn clean package -q

    - name: Generate release notes
      if: startsWith(github.ref, 'refs/tags/v')
      run: |
        echo "## What's Changed" > notes.md
        echo "" >> notes.md
        git log --oneline -10 --pretty=format:"- %s" >> notes.md
        echo "" >> notes.md
        echo "" >> notes.md
        echo "---" >> notes.md
        echo "## Installation" >> notes.md
        echo "1. Download the JAR file below" >> notes.md
        echo "2. Place it in your server's \`plugins/\` folder" >> notes.md
        echo "3. Restart the server" >> notes.md

    - name: Upload JAR
      uses: actions/upload-artifact@v4
      with:
        name: dcustomitems-${{ steps.version.outputs.version }}
        path: target/*.jar

    - name: Create Release
      if: startsWith(github.ref, 'refs/tags/v')
      uses: softprops/action-gh-release@v2
      with:
        name: "DC-CustomItems v${{ steps.version.outputs.version }}"
        body_path: notes.md
        files: target/*.jar
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
